name: dotnet package

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: ['5.0.x' ]

    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
    
    
      - name: Checkout Hub
        uses: actions/checkout@v3
        with:
          repository: 'artalexmay/RL_Test3'
          ref: ${{ github.ref }}
          path: 'RL_Test3'
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Checkout RL Shared
        uses: actions/checkout@v3
        with:
          repository: 'artalexmay/RL_Test_Shared'
          ref: ${{ env.SHARED_REF }}
          path: 'RL_Test_Shared'
          token: ${{ secrets.GITHUB_TOKEN }}
    
    
    
      - uses: actions/checkout@v3
      - name: Setup .NET Core SDK ${{ matrix.dotnet-version }}
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: ${{ matrix.dotnet-version }}

      - name: Get Shared Pull Requests
        uses: octokit/request-action@v2.x
        id: get_shared_prs
        with:
          route: GET /repos/{owner}/{repo}/pulls
          owner: artalexmay
          repo: RL_Test_Shared
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#      - name: Checkout RL_Test3
#        uses: actions/checkout@v3
#        with:
#          repository: 'artalexmay/RL_Test3'
#          ref: ${{ github.ref }}
#         path: 'RL_Test3'
#          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Checkout RL_Test_Shared
        uses: actions/checkout@v3
        with:
          repository: 'artalexmay/RL_Test_Shared'
          path: 'RL_Test_Shared'
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: List all files
        run: | 
            pwd 
            ls -R
        working-directory: ${{ env.GITHUB_WORKSPACE }}
          
      - name: RL ath
        run: |
            echo "::group::My title"
            echo "$GITHUB_PATH"
          
      - name: RL pwd
        run: pwd
        working-directory: ${{ env.GITHUB_WORKSPACE }}
          
 #     - name: Install dependencies
 #       run: dotnet restore
 
      - name: Restore dependencies
        run: dotnet restore TestProject1 -s https://api.nuget.org/v3/index.json
        working-directory: ${{ env.GITHUB_WORKSPACE }}
        
      - name: Build
        run: dotnet build TestProject1 --configuration Release --no-restore
        
      - name: Test
        run: dotnet test TestProject1 -c Release --no-build --verbosity n --logger trx
        continue-on-error: true

      - name: Publish test results
        uses: bibipkins/dotnet-test-reporter@c13c759e2c509efa3d83109f62dbb3e92697a140
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          #test-results: ./TestProject1/TestResults
          test-results: TestProject1/TestResults
          comment-title: 'Unit Test Results'
